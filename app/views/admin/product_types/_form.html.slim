= form_for [:admin, @product_type] do |f|
  = render 'shared/error_messages', object: @product_type

  .form-group
    = f.label :name
    = f.text_field :name, class: 'form-control'

  .form-group
    = f.submit class: 'btn btn-default'

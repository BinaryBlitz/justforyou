В мобильном приложении Just For You назначена доставка:

Пользователь: <%= @purchase.user.full_name %>

Желаемые даты доставки:
<%- @deliveries.each do |delivery| %>
  <%= "#{formatted_scheduled_for(delivery)} #{delivery.address}" %>
<% end %>

Состав заказа:
<%= "#{@purchase.program.name} x #{@purchase.number_of_days} #{t(@purchase.program.unit)}" %>
